import styled from "styled-components"

export const LeftMenuManageStyled = styled.div`
  padding: 40px 0 0;

  display: flex;
  flex-direction: column;

  .ant-menu {
    background: #f0f2f5;
  }

  .ant-menu-submenu-title .ant-menu-title-content {
    display: inline-block;
    margin: 0 0 10px 22px;
    font-weight: 700;
  }
`
